import {makeStyles} from 'tss-react/mui'

const useStyles = makeStyles((theme) => ({
      container : {
        color : 'red'
      },
      icon : {
        marginRight : '20px'
      },
      media :{
        height : '1'
      }
}))

export default useStyles